<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A Full-Stack App that allows you to publish and manage blog posts.">
    <title>Tech Blog</title>
  
    <!-- Bootstrap core CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }
      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
    </style>

    
    <!-- Custom styles for this template -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@200&display=swap" rel="stylesheet">
    <!-- Custom styles for this template -->
    <link href="https://tech-blog-ku.herokuapp.com/stylesheets/style.css" rel="stylesheet">
  </head>

<body id="top" style="background-color:#F8F8FF
">
  <div class="container">
    <header class="blog-header py-3">
      <div class="row flex-nowrap justify-content-between align-items-center">
        <div class="col-4 pt-1">
          <a class="link-secondary" href="/admin">Dashboard</a>
        </div>
        <div class="col-4 text-center">
          <a class="blog-header-logo text-dark" href="/">Check Out the Tech Blog</a>
        </div>
        <div class="col-4 d-flex justify-content-end align-items-center">
          {{#if session.loggedIn}}
          <button id="logout" class="btn btn-sm btn-outline-dark">logout</button>
          {{else}}
          <a class="btn btn-sm btn-outline-dark" href="/login">login</a>
          {{/if}}
        </div>
      </div>
    </header>

   <div class="nav-scroller py-1 mb-2">
      <nav class="nav d-flex justify-content-between">
          <a class="p-2 link-secondary" href="/admin/new">New Post</a>
          <a class="p-2 link-secondary" href="/">Feed</a>
      </nav>
    </div> 
  </div>
    <main class="container">
       <div class="row g-5">
        <div class="col-md-12">
          <h3 class="pb-4 mb-4 border-bottom">
            Your Recent Posts
          </h3>
          <section class="my-5">
            {{{ body }}}
          </section>
        </div>
      </div>
    </main>
<footer class="blog-footer">
  <p>Leave a Comment!</p>
  <p>
    <a href="#top">Back to top</a>
  </p>
</footer>
  
  {{#if session.loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>